export default class AfazerObj {

    constructor(name, path, price, local, desc) {
        this.name = name
        this.path = path
        this.price = price
        this.local = local
        this.desc = desc
    }
}